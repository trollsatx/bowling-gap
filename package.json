{
  "name": "bowling-gap",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/kosi-parser.test.js && node test/session-log.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
